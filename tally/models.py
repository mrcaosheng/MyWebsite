from django.db import models

# Create your models here.
# class Consumption(models.Model):
# 	consum_type = models.CharField(version_name="", null=True)
# 	account = models.FloatField(version_name="", null=True)